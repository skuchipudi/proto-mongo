package com.example.proto;

public class PropertilesLoad {

	public void load()
	{
		// TEMPORARY - 
				// Populate this from the RegistrationInfo
				/*
				String propertiesFilename = "config.properties";
				_props.load(new FileInputStream("properties/" + propertiesFilename));
				
				String serverAddress = _props.getProperty("serverAddress").trim();
				String userId = _props.getProperty("userId").trim();
				String password = _props.getProperty("password").trim();
				String remoteDirectory = _props.getProperty("remoteDirectory").trim();
				String localDirectory = _props.getProperty("localDirectory").trim();
				*/
		
	}
	
}
